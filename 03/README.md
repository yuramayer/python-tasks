# Consecutive integers

You're given the array of the integers. Return the length of the longest subarray with elements form a sequence of consecutive numbers, any order without duplicates.

The result should be O(n), so without sorting, as sort = O(n log n)
