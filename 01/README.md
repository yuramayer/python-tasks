# Строки и хеш-таблицы

Дана строка `s`. Напишите функцию, которая возвращает `true`, если в `s` есть подстрока, содержащая все уникальные символы строки без повторений (т. е. «самую длинную цепочку без повторов»). Какова будет её временная и пространственная сложность?

## Решение за $O(n^2)$

Решение за $O(n^2)$ в Ноутбуке `t1.ipynb`
